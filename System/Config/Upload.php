<?php
    // Don't Allow Access Directly
    if (!defined("ROOT")) { exit(); }

    // Server One
    define("UPLOAD_SERVER_1", "127.0.0.1", true);
    
    // Server One Token
    define("UPLOAD_SERVER_1_TOKEN", "AccessToken", true);

    // Server Two
    define("UPLOAD_SERVER_2", "127.0.0.1", true);

    // Server Three
    define("UPLOAD_SERVER_3", "127.0.0.1", true);
?>