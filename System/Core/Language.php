<?php
    // Don't Allow Access Directly
    if (!defined("ROOT")) { exit(); }

    $Lang["DATABASE_ERROR_CON"] = 1;
    // DB Reserve 2
    // DB Reserve 3
    // DB Reserve 4
    // DB Reserve 5
    // DB Reserve 6
    // DB Reserve 7
    // DB Reserve 8
    // DB Reserve 9
    // DB Reserve 10
    // DB Reserve 11
    // DB Reserve 12
    // DB Reserve 13
    // DB Reserve 14
    // DB Reserve 15

    $Lang["AUTH_ERROR_EMPTY_TOKEN"] = 16;
    $Lang["AUTH_ERROR_EMPTY_DATA"] = 17;
    $Lang["AUTH_ERROR_EXPIRED_TOKEN"] = 18;
    $Lang["AUTH_ERROR_CANNOT_SIGN"] = 19;
    $Lang["AUTH_ERROR_WRONG_SEGMENT_COUNT"] = 20;
    $Lang["AUTH_ERROR_VERIFY_FAILED"] = 21;
    $Lang["AUTH_ERROR_OPENSSL_VERIFY_FAILED"] = 22;
    // Auth Reserve 23
    // Auth Reserve 24
    // Auth Reserve 25
    // Auth Reserve 26
    // Auth Reserve 27
    // Auth Reserve 28
    // Auth Reserve 29
    // Auth Reserve 30

?>