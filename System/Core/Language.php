<?php
    // Don't Allow Access Directly
    if (!defined("ROOT")) { exit(); }

    // General Lang
    $Lang["GEN_SUCCESS"]          = 1000;
    $Lang["GEN_LONG_USERNAME"]    = 800;
    $Lang["GEN_LONG_PASSWORD"]    = 801;
    $Lang["GEN_SHORT_USERNAME"]   = 802;
    $Lang["GEN_SHORT_PASSWORD"]   = 803;
    $Lang["GEN_EMPTY_USERNAME"]   = 804;
    $Lang["GEN_EMPTY_PASSWORD"]   = 805;
    $Lang["GEN_INVALID_USERNAME"] = 806;

    $Lang["DATABASE_CONNECTION"] = 1;
    // DB Reserve 2
    // DB Reserve 3
    // DB Reserve 4
    // DB Reserve 5
    // DB Reserve 6
    // DB Reserve 7
    // DB Reserve 8
    // DB Reserve 9
    // DB Reserve 10

    $Lang["AUTH_EMPTY_TOKEN"]           = 11;
    $Lang["AUTH_EMPTY_DATA"]            = 12;
    $Lang["AUTH_EXPIRED_TOKEN"]         = 13;
    $Lang["AUTH_CANNOT_SIGN"]           = 14;
    $Lang["AUTH_WRONG_SEGMENT_COUNT"]   = 15;
    $Lang["AUTH_VERIFY_FAILED"]         = 16;
    $Lang["AUTH_OPENSSL_VERIFY_FAILED"] = 17;
    // Auth Reserve 18
    // Auth Reserve 19
    // Auth Reserve 20
    // Auth Reserve 21
    // Auth Reserve 22
    // Auth Reserve 23
    // Auth Reserve 24
    // Auth Reserve 25
    // Auth Reserve 26
    // Auth Reserve 27
    // Auth Reserve 28
    // Auth Reserve 29
    // Auth Reserve 30

    $Lang["REGISTER_EMPTY_EMAIL"]            = 31;
    $Lang["REGISTER_INVALID_EMAIL"]          = 32;
    $Lang["REGISTER_LONG_EMAIL"]             = 33;
    $Lang["REGISTER_ALREADY_EXIST_USERNAME"] = 34;
    $Lang["REGISTER_ALREADY_EMAIL"]          = 35;
    // Register Reserve 36
    // Register Reserve 37
    // Register Reserve 38
    // Register Reserve 38
    // Register Reserve 39
    // Register Reserve 40

    $Lang["LOGIN_EMPTY_SESSION"]           = 41;
    $Lang["LOGIN_NOT_EXIST_USERNAME"]      = 42;
    $Lang["LOGIN_WRONG_USERNAME_PASSWORD"] = 43;
    // Login Reserve 44
    // Login Reserve 45
    // Login Reserve 46
    // Login Reserve 47
    // Login Reserve 48
    // Login Reserve 49
?>